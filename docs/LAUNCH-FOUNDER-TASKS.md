# Launch Founder Tasks — Week 1 (created 2026-09-18)

Founder-only tasks from the Launch-Week Master Brief v1.1, Appendix A. Agents append here as new founder actions are discovered; they never do these tasks themselves.

- [ ] **Stripe live mode**: complete identity verification; create products/prices: "Tome — Student Seat" ($12 recurring yearly, per-unit; used by both Classroom and School with `metadata.tier` set at checkout), "Tome School — up to 150 students" ($1,800 recurring yearly, `metadata.tier=school`, `metadata.student_seats=150`), "Tome School — up to 300 students" ($3,200 recurring yearly, `metadata.tier=school`, `metadata.student_seats=300`), a new $99 yearly price on the existing Family product (`prod_UnOy09CqGKXxB4`, `metadata.tier=family`), "Tome Questions — 10,000" ($149 one-time, `metadata.kind=topup`); paste the price ids into Vercel as `TOME_PRICE_STUDENT_SEAT_YEARLY`, `TOME_PRICE_SCHOOL_FLAT_150`, `TOME_PRICE_SCHOOL_FLAT_300`, `TOME_PRICE_FAMILY_YEARLY`, `TOME_PRICE_QUESTIONS_TOPUP`; **register the webhook endpoint** `https://<domain>/api/stripe/webhook` with events `checkout.session.completed`, `customer.subscription.created/updated/deleted`, `customer.subscription.trial_will_end`, `invoice.paid`, `invoice.payment_failed`, `invoice.upcoming`; paste the signing secret; archive the Solo product (`prod_UnOyR0U75npmXt`) and the old Family monthly/yearly prices (keep the Family product; its one live subscriber stays on the old price until they change).
- [ ] **Resend**: add the DNS records from `docs/email-setup.md` for `usetome.app` (DKIM `resend._domainkey`, SPF + MX on `send`, DMARC on `_dmarc`), verify, set `RESEND_API_KEY` in Vercel; set up inbound/forwarding for `support@usetome.app`; set `EMAIL_POSTAL_ADDRESS` (registered business address — CAN-SPAM).
- [ ] **Vercel env**: `LIVEBLOCKS_SECRET_KEY` (+ public key), `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`, `ADMIN_USER_IDS`, all of Phase 10.3; attach the final domain.
- [ ] **Supabase Auth**: enable leaked-password protection (Auth → Password security).
- [ ] **Legal**: counsel review of `/privacy`, `/terms`, `/security` (COPPA April-2026 amendments: written retention policy, named sub-processors, no bundled consent, automated-profiling disclosure); sign the SDPC NDPA v2.2; confirm the FAQ claims moved here from `faqs.ts` (refund window, payment methods, ESA distribution, device support, "data not used to train models").
- [ ] **Texas TEFA / Odyssey**: file Form 304 (foreign entity) with the Texas SOS if Tome is not a Texas entity; obtain Certificate of Fact and Comptroller good standing; connect Stripe payout; submit the vendor application **now** (rolling review by Odyssey and the Comptroller, no fee, Net-30); list the **Family plan ($99 / year, up to 4 students)** under **"Curriculum"** and/or **"Online Learning Program"**, never "Software Programs" (10 % technology cap); the offering text must state what is included, the quantity and the service period ("full academic year"); confirm the cap treatment with Odyssey support in writing.
- [ ] **Assets**: approve the hero painting (A / B / C) and the rotation set; confirm the email watermark set.
- [ ] **Branches**: decide on `today/virgil-surfaces-functional`, `today/stripe-full-integration`, `fix/stripe-webhook-proof` (merge after diff, or delete); delete `consumer` and `feat/se-cover-art` (fully merged/behind).
- [ ] **Stripe TEST mode** (unblocks `npm run verify:billing:stripe`, Gate 2): create test-mode counterparts of the launch prices and add to `.env.local` — missing today: `TOME_PRICE_STUDENT_SEAT_YEARLY`, `TOME_PRICE_SCHOOL_FLAT_300`, `TOME_PRICE_QUESTIONS_TOPUP` (`TOME_PRICE_FAMILY_YEARLY` and `TOME_PRICE_SCHOOL_FLAT_150` are already set). The script skips cleanly until then and refuses live keys.
- [ ] **Gate 2 manual UI pass** (server-side enforcement already proven by `verify:billing` / `verify:questions`; these are the browser confirmations): (1) as a free teacher with one classroom, "Create classroom" shows "The free teacher plan includes one classroom. Add student seats to create more."; (2) generate a quiz in the quiz builder and watch the Questions Available chip drop by the requested count; (3) from `/account/school`, invite a teacher by email, claim the seat, and confirm that teacher's classroom joins draw from the school seat allowance.
- [ ] **Test purchase** in live mode with a 100 % coupon, then refund.
- [ ] **Anthropic API credits** (blocks Gate 3 full-canon quiz backfill): the account ran out of credits mid-run on 2026-09-18 (~65 of ~1,180 books completed; every later call 400s "credit balance is too low"). Budget ≈ $250–300 (Sonnet, ~3,800 calls). After topping up, resume with `npx tsx scripts/quizzes/rebalance-types.ts --scope=all --commit` (idempotent — the `quiz_generation_runs` ledger skips finished books), then run `scripts/canon-quiz-validate.ts` on a random 25.
