// Platform (pre-generated) book quizzes — the reader end-of-chapter Trials and
// the free three-tier practice surface. Re-enabled for launch (1.6): the
// assignment loop resolves platform banks as the default end-of-range quiz.
// Teacher-created quizzes and class assignments never depended on this flag.
export const PLATFORM_QUIZZES_ENABLED = true
